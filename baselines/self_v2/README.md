Frozen snapshot of the **numba bitboard** engine (Stage 2, 1 Sep 2026).

This directory is a legal harness opponent. Use it as the A/B baseline for Stage 3 (NNUE):

```
python -m harness.arena --opponent baselines/self_v2 --games 40 --base-ms 10000
```

Do not edit these files.
