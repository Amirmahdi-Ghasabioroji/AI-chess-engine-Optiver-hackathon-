"""Competition engine: bitboard search, PeSTO eval, optional learned residual.

The platform imports `agent` from the zip root. That module is a thin
orchestration layer; everything it calls lives in this package.
"""
